
1. 原生js获取



2. jquery获取
```js
// 选中父类标签
const trs = $(dom).find("xxx");
const arr = new Array(trs.length);
for(let i = 0; i < trs.length; i ++) {
    // 选中每一个父类标签下，单选选中的内容
    const val = $(trs[i]).find(":checked").val();
    arr[i] = val;
}
console.log(arr);
```